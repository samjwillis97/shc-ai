import { describe, it, expect } from 'vitest';

describe('Dummy Test', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });

  it('should handle basic math', () => {
    expect(2 + 2).toBe(4);
  });
}); 